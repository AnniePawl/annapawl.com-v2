// Moved to src/lib/cx.ts so shared components (outside vibe-system) can use
// it too. Re-exported here for backward compatibility — nothing in
// vibe-system imports from this file anymore, so it's safe to delete
// manually next time you're in the editor.
export { cx } from "../../../lib/cx";
